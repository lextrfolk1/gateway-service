server:
  port: 8081

spring:
  cloud:
    gateway:
      routes:
        - id: testing-service
          uri: http://testing-service
          predicates:
            - Path=/**
          filters:
            - RewritePath=/proxy/(?<segment>.*), /${segment}
